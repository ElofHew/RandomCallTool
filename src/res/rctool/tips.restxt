=== 简介与主要功能 ===
# 一、主要功能
#
1. 随机抽组
   从指定数量的组（数字或字母）中随机抽取一个或多个组。
#
2. 随机抽人
   从样本文件（.rcp / .txt / .csv）或样本库中
   随机抽取指定数量的人名。
#
3. 三档抽样模式
   · 基本抽样：random.sample 简单随机抽取
   · 智能抽样：追踪近期历史，自动降低刚被选中项的权重
   · 加权抽样：可单独设置权重，权重越高概率越大
#
4. 样本库管理：常用名单导入样本库（上限 50 个）
5. 整合式操作界面：抽人与抽组一键切换
6. 历史记录面板：支持单条保存和批量保存
7. 配置管理：多选项卡配置窗口
8. 快捷键操作：Ctrl+Enter 抽取 / Ctrl+O 选择文件等
=== 操作指南 — 随机抽组 ===
# 二、操作指南
#
# 【随机抽组】
#
1. 切换到「随机抽取」选项卡，选择「随机抽组」模式
#
2. 选择分组方式：
   · 数字（123）：组编号为 1, 2, 3 ...
   · 字母（ABC）：组编号为 A, B, C ...
#
3. 设置样本总数（1-26）
#
4. 设置要抽取的组数（不能超过总组数）
#
5. 点击「抽取」按钮或按 Ctrl+Enter 进行抽取
#
6. 结果展示在程序窗口，可保存为 HTML 文件
=== 操作指南 — 随机抽人 ===
# 【随机抽人】
#
1. 切换到「随机抽取」选项卡，选择「随机抽人」模式
#
2. 加载样本文件（支持 .rcp / .txt / .csv）：
   · Ctrl+O       → 手动选择文件
   · Ctrl+Shift+O → 从样本库加载
   · Ctrl+R       → 重新加载当前文件
   · Ctrl+D       → 自动加载默认样本
#
3. 设置抽取数量
#
4. 选择抽样模式（基本 / 智能 / 加权）
#
5. 点击「抽取」按钮或按 Ctrl+Enter 进行抽取
#
6. 结果展示在程序窗口，可保存为 HTML 文件
=== 文件格式说明 ===
# 三、文件格式说明
#
1. .rcp 文件（推荐）
   经过 Base64 编码的名单文件，
   使用配套的「名单编码工具」生成。
#
2. .txt 文件
   纯文本文件，每行一个名字，
   也支持逗号、分号、制表符分隔。
#
3. .csv 文件
   逗号分隔值文件。
#
4. 重复名字处理：
   可在配置中开启「合并重复名字」功能，
   开启后自动去除重复项。
=== 抽样模式与附加功能 ===
# 四、智能抽样功能
#
1. 抽取前打乱：提高随机性，减少顺序影响
2. 加权抽样：根据历史抽取次数调整权重，实现长期公平
3. 可重置抽样历史：重新开始公平性计算
#
# 五、其他功能
#
1. 结果保存：抽取结果自动保存为 HTML 格式
2. 历史记录：记录最近的抽取历史，支持查看和保存
3. 配置管理：可设置保存路径、自动保存、默认值等
4. 日志查看：记录程序运行状态，支持查看和清理
5. 自动检测更新：启动时静默检测新版本
6. 快捷键操作：全程键盘可完成大部分操作
=== 注意事项 ===
# 六、注意事项
#
1. 支持重复名字自动去重（可在配置中关闭）
2. 抽取人数不能超过样本总数
3. 默认样本文件存放在 data/default.rcp
4. 结果文件默认保存在 data/result 或桌面
5. 支持跨平台运行（Windows / Linux / macOS）
